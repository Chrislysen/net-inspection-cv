# Core (required) — data loading, metrics, IO
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
pillow>=10.0

# Computer-vision baseline, visualisation, video (recommended)
opencv-python-headless>=4.8
matplotlib>=3.7
scikit-image>=0.21
scikit-learn>=1.3

# ML baseline (optional; pulls in torch). Comment out if you only need the
# classical baseline + evaluation. torchvision powers the PatchCore anomaly model.
ultralytics>=8.1
torchvision>=0.15

# Real-data ingestion: read SOLAQUA ROS .bag files without a ROS install.
rosbags>=0.9

# Serving (FastAPI inference service) + interactive viewer (Streamlit).
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
streamlit>=1.33
httpx>=0.27

# Tests
pytest>=7.4
